


def initials(firstname, secondname):
    # firstinitial = firstname[0]
    # secondinitial = secondname[0]
    # initials = firstinitial + secondinitial
    # return initials.upper()
    return (firstname[0] + secondname[0]).upper()
    
print(initials(secondname="bundil", firstname="peta")